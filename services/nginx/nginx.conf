upstream led_server {
    server collection_leds:5000;
}

server {
    listen 80 default_server;
    listen [::]:80 default_server;

    root /var/www/html;
    index index.php index.html index.htm index.nginx-debian.html;

    server_name home.sakoarts.nl;

    location / {
        try_files $uri $uri/ =404;
    }
    location /collection {
        #auth_basic           "AdministratorÃ¢â‚¬â„¢s Area";
        #auth_basic_user_file /etc/apache2/.htpasswd;
	    #proxy_pass http://127.0.0.1:5000;
        #proxy_pass http://unix:/home/sako/led_server/ledserver.sock;
        proxy_pass http://led_server;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header Host $host;
        proxy_redirect off;
    }

    # location /wake_pc {
    #     auth_basic           "AdministratorÃ¢â‚¬â„¢s Area";
    #     auth_basic_user_file /etc/apache2/.htpasswd; 
    # }


    # location ~ \.php$ {
    #     include snippets/fastcgi-php.conf;
    #     fastcgi_pass unix:/run/php/php7.3-fpm.sock;
    # }

    # location ~ /\.ht {
    #     deny all;
    # }

    #listen 443 ssl; # managed by Certbot
    #ssl_certificate /etc/letsencrypt/live/home.sakoarts.nl/fullchain.pem; # managed by Certbot
    #ssl_certificate_key /etc/letsencrypt/live/home.sakoarts.nl/privkey.pem; # managed by Certbot
    #include /etc/letsencrypt/options-ssl-nginx.conf; # managed by Certbot
    #ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem; # managed by Certbot


}
